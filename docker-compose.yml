services:
  app:
    build: .
    ports:
      - "8080:8080"
    environment:
      - OPENAI_API_KEY=${OPENAI_API_KEY}
      - PORT=8080
      - DB_PATH=/data/quiz.db
    volumes:
      - quiz-data:/data

volumes:
  quiz-data:
